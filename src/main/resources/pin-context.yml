uses: [ "button.yml", "popup.yml" ]
type: element
id: pin-context
content:
  type: popup
  id: pin-context
  elements:
    - type: button
      label: "Rename"
      event: rename
    - type: button
      label: "Delete"
      event: delete