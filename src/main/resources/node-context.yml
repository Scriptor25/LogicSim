uses: [ "button.yml", "popup.yml" ]
type: element
id: node-context
content:
  type: popup
  id: node-context
  elements:
    - type: button
      label: "Rename"
      event: rename
    - type: button
      label: "Add Input"
      event: input
    - type: button
      label: "Add Output"
      event: output
    - type: button
      label: "Delete"
      event: delete